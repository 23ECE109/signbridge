# SignBridge — Runtime Requirements
# Install: pip install -r requirements.txt
# Python 3.10 or 3.11 recommended (standard x64 Windows Python is fine for dev)

# ── Core vision ───────────────────────────────────────────────────────────────
mediapipe==0.10.14          # Hand/body/face landmark extraction (CPU, works immediately)
opencv-python==4.10.0.84    # Camera capture + frame rendering

# ── UI ────────────────────────────────────────────────────────────────────────
PyQt6==6.7.1                # Main application window

# ── Speech recognition (partner ASR) ─────────────────────────────────────────
openai-whisper==20240930    # Whisper speech-to-text (CPU mode, downloads ~150 MB once)
sounddevice==0.5.1          # Microphone input + audio playback
soundfile==0.12.1           # WAV read/write

# ── Text-to-speech ────────────────────────────────────────────────────────────
pyttsx3==2.90               # Windows SAPI5 TTS — works with zero download

# ── Storage & security ────────────────────────────────────────────────────────
cryptography==42.0.8        # AES-256 encryption for user profiles

# ── Utilities ─────────────────────────────────────────────────────────────────
numpy==1.26.4
scipy==1.13.1               # cosine similarity for correction memory
tqdm==4.66.4

# ── Optional: Snapdragon NPU inference (install on Snapdragon HP PC only) ─────
# onnxruntime==1.18.1       # Uncomment for ONNX model inference
# pip install onnxruntime   # QNN provider is included on Snapdragon Windows
